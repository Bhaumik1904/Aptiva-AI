# Redrob Hackathon — Submission Metadata Template
#
# Copy this file to your repo root as `submission_metadata.yaml` and fill it in.
# The fields here should match what you submit via the portal at upload time.
# Stage 3 review uses this file to verify your portal metadata.

# ============================================================================
# Team identity
# ============================================================================
team_name: "Cyber Shift"  # Used in leaderboard and announcements

primary_contact:
  name: "Bhaumik Hinunia"
  email: "bhaumikhinunia019@gmail.com"   # Used for all organizer communication
  phone: "+91-9521144404"        # Used for top-50 / top-10 outreach

team_members:
  - name: "Bhaumik Hinunia"
    email: "bhaumikhinunia019@gmail.com"
    role: "Team Lead • ML Engineer • Full Stack Developer"          # Optional, e.g. "Team Lead", "Backend", "Data"
  - name: "Harsh Dathik"
    email: "harshdathik2006@gmail.com"
    role: "AI Engineer • Evaluation & Testing"
  - name: "Manya Srivastava"
    email: "manya_srivastava@srmap.edu.in"
    role: "UI/UX Designer • Documentation"
  - name: "Tanuj Choudhary"
    email: "tanuj162007@gmail.com"
    role: "Research • Quality Assurance"
  # Add more members as needed; solo participants list just one member

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/Bhaumik1904/Aptiva-AI"
# Required. Must be reachable. Private repos OK if you can grant organizer
# access at Stage 3 (the email to add will be communicated then).

sandbox_link: "https://aptiva-ai.streamlit.app/"
# Required. A working hosted environment where the ranker can be run on a small
# candidate sample. See Section 10.5 of submission_spec.md for acceptable
# platforms (HuggingFace Spaces, Streamlit Cloud, Replit, Colab, Docker, Binder).

reproduce_command: |
  # CLI Pipeline (generates .xlsx directly)
  python rank.py --candidates data/candidates.jsonl --out submission.xlsx
  
  # Interactive Web Application
  streamlit run app.py
# Should run end-to-end within 5 minutes on CPU with 16GB RAM and no network.

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Local Windows Development Environment"             # Or: "AWS EC2 c5.4xlarge", "Local Linux box", etc.
  cpu_cores: 8                                  # Number of CPU cores used
  ram_gb: 16                                    # Available RAM in GB
  python_version: "3.11.6"                        # python --version
  os: "Windows 11"                              # Or "Ubuntu 22.04 LTS", etc.
  uses_gpu_for_inference: false                 # Must be false — see compute constraints
  has_network_during_ranking: false             # Must be false — no API calls during ranking
  pre_computation_required: false               # true if you pre-compute embeddings or train models offline
  pre_computation_time_minutes: 0               # Approximate, if applicable

# ============================================================================
# AI tools declaration
# ============================================================================
# Transparency only — declared use is NOT penalized. Be honest. Stage 5 interview
# may verify these declarations against your code; declarations that contradict
# your code or your interview are flagged.
ai_tools_used:
  - "ChatGPT"
  - "Claude"
  - "Gemini"
  # Other options: "ChatGPT", "Cursor", "Gemini", "Codeium", "Other", "None"

ai_usage_summary: |
  We utilized ChatGPT, Claude, and Gemini strictly as development assistants to support brainstorming, debugging, documentation, code review, and UI refinement.
  
  All implementation, integration, testing, and engineering decisions were performed entirely by the team. The candidate ranking algorithm is fully deterministic, CPU-only, and does not use LLMs during inference or scoring.

# ============================================================================
# Approach summary (optional but recommended)
# ============================================================================
methodology_summary: |
  We developed a highly optimized, multi-signal ranking engine using deterministic CPU-only evaluation. We combine a TF-IDF based retrieval system with explicit explainable AI reasoning. The core of our solution is the Hireability Index, capturing holistic candidate value without relying on LLMs for core ranking decisions. A sophisticated evaluation framework ensures fairness and precision, while our interactive Streamlit deployment provides a recruiter-focused interface to analyze signals in real time.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true        # I have read submission_spec.md in full
  code_is_original_work: true       # My code is my team's original work (using AI as a tool is fine)
  no_collusion: true                # I have not coordinated my submission with other teams
  honeypot_check_done: false        # OPTIONAL — set true if you explicitly checked for honeypots in your ranking
  reproduction_tested: true         # I have tested that my reproduce_command runs end-to-end
